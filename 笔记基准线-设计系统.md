# 笔记基准线 · 设计系统(反向抽取自 sdd 参考)

这份文件把参考项目 `sdd/`(一个教程 skill 的产出)里的**视觉与格式纪律**抽出来,定成我们这套学习笔记的基准线。
它只管「长什么样、怎么排」;「写什么、外链怎么策展」由同库的 `写作规矩-外链策展法.md` 管,两者合起来才是完整的 skill。

样章:`基础课程/02-Python基础.html`(2026-08-13 按本系统重做,自足单文件、离线双击即看)。

---

## 一、一条总纲:靠「结构」分级,不靠「颜色」分级

参考之所以干净,是因为它把「区分信息」的活全交给了**版式结构**(字号、留白、顶边线、等宽小标签、左竖线),
颜色几乎不参与分级。我们之前的毛病正相反:用五套颜色去区分内容块,于是「杂、乱、不标准」。

**记住一句:每加一种颜色前,先问能不能用『线 / 字号 / 小标签』解决。**

---

## 二·前、字号只有 6 级(2026-08-13 血泪补:治「字号过多」)

第一版栽在这:CSS 里硬写了 9/10/11/11.5/12/12.5/13/14/14.5/15/15.5/16/22/30 十四种字号,
行内代码又用 `.87em` 套在不同父级里滚出 11.31、11.745、12.18… 一堆怪数字——实测 **19 种字号**,页面必然「乱」。
**药方:全站只允许这 6 级,写死成变量,任何元素只能取其一,不许再冒别的数字。**

| 变量 | 值 | 用途 |
|---|---|---|
| `--fs-micro` | 11px | 一切等宽小标签 / 图注 / 目录 / 面包屑 |
| `--fs-small` | 13px | 表格 / 代码 / 次要说明 |
| `--fs-body` | 15px | 正文 |
| `--fs-lead` | 16px | 一句话定义 / 导语 |
| `--fs-h3` | 20px | 子标题 |
| `--fs-h2` | 25px | 小节标题 |
| `--fs-h1` | 34px | 章标题(全页仅一个) |

> **2026-08-30 更正**:上表原为 18/22/30。2026-08-19 的增补已把标题档提到 **20/25/34**
> (理由:和 15px 正文拉开档,一眼看出是标题),全站 63 页中 62 页已跟上,
> **只有样章 `02-Python基础.html` 还停在旧值,现已补齐**。下文所有「18/22/30」的提法一律作废。

三条铁律:①**行内代码固定 `13px`,绝不用 `em`**(em 会层层相乘出怪数)。②**绝不在正文写 `style="font-size:…"`**——散落的内联样式是「字号过多」的第二大来源,一切样式进 CSS 类。③**图(SVG)里没写字体的 `<text>` 会回落 Arial**,必须加一条 `figure svg text{font-family:var(--sans)}` 兜底,否则页面凭空多一种字体。核验:浏览器里数一遍正文字号,必须 =7(11/13/15/16/**20/25/34**),字体必须 =2(Noto Sans SC + JetBrains Mono)。

## 二、配色:一个强调色 + 灰阶(我们选群青蓝)

| 变量 | 值 | 用途 |
|---|---|---|
| `--bg` | `#eef1f6` 冷灰蓝纸 | 页面底 |
| `--surface` | `#ffffff` | 卡片 / 代码 / 图 的底 |
| `--raise` | `#f6f8fc` | 说明块的浅底 |
| `--ink` | `#14181f` | 正文墨色 |
| `--ink-soft` | `#55617a` | 次要文字 |
| `--ink-faint` | `#8b95a8` | 更淡(序号、图里的说明) |
| `--rule` | `#d8dee9` | 分隔线 / 描边 |
| `--acc` | `#2743c7` 群青蓝 | **唯一强调色** |
| `--acc-soft` | `rgba(39,67,199,.07)` | 强调块的浅底 |

**铁律:整页只有这一个彩色(群青蓝)。** 其余全走墨黑 / 三级灰。
代码高亮也克制:关键词=强调蓝、字符串=一个哑绿 `#2f6f52`、注释=淡灰斜体,数字和其它一律走墨色。全页彩色种类 ≤ 2。

---

## 三、字体两套,靠「衬线对比」立层级

- 正文:`Noto Sans SC / 微软雅黑 / 系统无衬线`(不挂 CDN,离线回落到系统中文字体)。
- 等宽:`JetBrains Mono / Cascadia Code / Consolas`。**所有小标签、序号、代码、图里的编号**都用等宽。

等宽小标签(10px、大写化、字距 `.1em`、灰色)是这套系统的招牌——它替代了「彩色标题」来标记每种块的身份。

---

## 四、层级只有四级,每级一种固定处理

| 级 | 处理 | 说明 |
|---|---|---|
| h1 章标题 | 30px 粗,章首区,下方一条 `--rule` 线 | 全页仅一个 |
| h2 小节 | 22px 粗 + **等宽灰序号** + **顶部一条 `--rule` 线** | 顶边线是分节的主视觉 |
| h3 子标题 | 16px 粗,无线 | |
| 一句话定义 | 16px + 左侧 `--ink-faint` 竖线 | 每个概念开头那句 |

**不准**:给标题上底色、上彩色、加下划线;不准同一页出现两种 h2 样式。

---

## 五、框只有 3 类,说明块共用一个骨架(治「框太多、格式过多」)

第一版每种说明块各写各的边框(rationale/callout/predict/challenge/one-liner/schema 六套),
任意一屏撞见十几种框和小标签。**收敛到只有 3 类「框」,不许再造第 4 类:**

1. **左线说明块**(rationale / callout / callout.go / callout.warn / predict / challenge):
   **共用同一段 CSS**——`背景 raise + 左线 3px + 统一内外距(--pad-block / --gap-block)+ 等宽小标签 + 正文`。
   种类之间**只差左线颜色**:中性=`--ink-faint`,交棒/提醒/想一想/挑战=`--acc`;warn 再加浅蓝底。别的一律相同。
2. **带边框的白底卡**(代码块 / 图 / 单步演示 sim):`1px 描边 + 6px 圆角 + 白底`,可选顶部标题条。
   **图一定属于这类**——每张 `figure` 都套这个框,散的内容被框收住,不再「飘」。
3. **纯细线表格**:表头下一条墨线、行间 `--rule` 细线,不填色、不加外框。

小标签也统一成**一个 token**:所有 `.label/.ql/.tg/.caption/.lang…` 走同一条 CSS
(等宽、11px、字距 .12em、大写、600 字重、灰色),不许各写各的字号。

### 内容块词典(每种块=一个等宽小标签 + 正文,长得像一家人)

所有说明块都是同一个骨架:`左竖线 + 顶部等宽小标签 + 正文`。只有「边框色」区分种类,且只有需要提醒时才上强调色。

| 块 | 小标签例 | 边框 | 用途 |
|---|---|---|---|
| `.rationale` | 为什么 / 画面 | 灰 | 解释「为什么需要它」 |
| `.callout` | 一句话记牢 / 定义≠执行 | 灰 | 中性提示 |
| `.callout.go` | 去精读 / 往后连 | **蓝** | 交棒给外部教程、连到后面章节 |
| `.callout.warn` | = 和 == / 越界会报错 | **蓝 + 浅蓝底** | 真正要拦住的坑 |
| `.predict` | 想一想 | **蓝** | 抛问题 + `<details>` 折叠答案 |
| `.challenge` | 进阶挑战 | **蓝** | 刚好够不着的迁移任务 |
| `.schema-panel` | 你会立起来的骨架 | 左粗墨线 | 章首「本章骨架」清单 |

配套:表格用细横线(`thead` 下一条墨线,行间 `--rule`),不填色;主线那一行用 `tr.sel` 左侧蓝竖线点一下。
代码用 `.code-block`(头部:文件名 + 语言 + 复制按钮)。折叠答案统一 `<details>`,三角 ▸ 展开转 90°。

---

## 六、图的统一画法(治「图逻辑排版乱」)

只用三种线 + 三种填充,全部走 CSS 工具类:

- 线:`.d-ink`(墨,结构)、`.d-acc`(蓝,当前重点)、`.d-soft`(灰虚线,被弱化/淘汰的东西)。
- 填充:`.nf`(白)、`.nf-acc`(浅蓝,重点节点)、`.nf-ink`(墨,表示「黑盒」)。
- 文字:`.nl`/`.nl-acc`/`.nl-inv`(节点标签)、`.el`/`.el-acc`/`.el-mono`(连线旁小字)。

**关键技巧——白色描边光晕**:连线旁的文字加 `paint-order:stroke; stroke:var(--bg); stroke-width:4px`,
文字就会被一圈底色包住,**永远不和线、不和别的字打架**。我们之前图里文字撞线,就是缺这一招。

其它:一个概念一张图;箭头用 `<marker>`;`viewBox` 上下各留 ~20px 余量防裁切;SVG 里 `font-family` 要写**具体字体名**(`var()` 在 SVG 呈现属性里不生效)。

---

## 六·后、代码与图不许手搓,走成熟工具预渲染(2026-08-13 定案)

手写 `<span>` 高亮和手画 SVG 是「不一致、格式过多」的病根。定案:**上成熟工具,构建期预渲染成离线静态文件,零运行时。**

**代码 = Shiki**(VS Code 引擎)。用自定义**纯墨黑灰主题(变体 A)**:注释 `#8b95a8` 斜体、字符串 `#557089`、关键词加粗墨色、其余墨色,**零彩色**(强调蓝只留给正文链接/警示)。`codeToHtml` 出的是自带内联样式的 HTML,直接塞进 `.code-block` 外壳,复制按钮照用。脚本见 `_工具/render/convert-code.mjs`。

**图 = Mermaid + 自制 SVG + 表/面板 混合**。**流程/关系类**(字典、if、for、函数机器)用 Mermaid 预渲染成静态 SVG 内联;**辐射/环绕类**(五块地基=中心目标+五块环绕)和**概念示意类**(变量=贴标签)Mermaid 做不了(它只会层级树,硬做会挤成扇形),一律**自制 SVG**,但复用 `.d-ink/.d-soft/.nf/.nf-acc/.nl/.nl-acc` 这套类保持和 Mermaid 一致的墨线白底+强调中心;**报错**本质是文本→做成 `.trace` 文本面板;**索引格子**本质是表→做成 `.idxgrid` 表格。脚本见 `_工具/render/render-diagrams.html` + `inject-diagrams.mjs` + `replace-fig1.mjs`。

**两个血泪坑(务必照做):**
1. **Mermaid 必须 `htmlLabels:false`**。默认的 `htmlLabels:true` 用 `foreignObject` 装 HTML 文字,内联进页面后会**继承页面 CSS(字号/行距/字距)**把文字撑宽、超出节点被截断。改用原生 SVG `<text>` 后免疫页面 CSS,`<br/>` 多行照常。
2. **渲染时字体要用本机装了的**(如 `Microsoft YaHei`),和页面实际回退字体一致;否则渲染器按别的字体量节点宽度,显示时又换字体 → 框比字窄 → 截字。
- 单色皮肤:节点白底墨线、连线灰、边标签白底光晕、强调只点一个群青蓝节点;**不要深色黑块**(用浅底 `#f4f6fb` + 蓝框表示重点)。

## 六·末、正文只有一条左边线(2026-08-14 血泪补:治「乱、没章法」)

真正让页面显得乱的,是**左边缘参差**:段落在 x=0,一句话定义缩进 14px,说明框缩进 21px,过渡句顶着个 `↓` 符号——五种左起点像锯齿,眼睛找不到基准。

**铁律:所有流动正文(段落、一句话定义、说明块、过渡句)的文字左边缘,一律对齐到同一条线(x=0)。**
做法=**悬挂竖线**:说明块用 `margin-left:-18px; padding-left:15px; border-left:3px`,竖线挂到版心外的沟槽里,文字回到 0,与段落齐。**去掉说明块的填充底色**(底色会逼文字缩进,还会漏进沟槽),块之间靠"竖线颜色 + 等宽小标签"区分,不靠底色方框。**不要在正文里塞方向符号**(`↓ →` 之类),它们顶破左边线。
例外只有两个,且是刻意的:列表项的项目符号、h2 的等宽节号——它们是"标记"不是"正文",可以带自己的缩进。

配套:**加粗要省**。过渡句一律不加粗;正文里每段最多一处加粗领句,别让一句话里三四个词全黑——加粗一多,视觉就发花。

**列表也要归到这条左边线**:`ul/ol` 去掉默认缩进和小圆点,标记(`—` 或等宽序号)悬挂到沟槽里(`li::before` 用 `position:absolute; left:-18~-24px`),列表文字与段落同一左边缘。不用 `•` 小圆点。

**行内代码底色要和页面拉开**:页面底 `#eef1f6`,行内 `code` 底色不能太近(`#e9edf5` 那种一撞就看不见)。用 `#dbe2ef`(明显但仍浅)+ 文字走 `--ink-soft`,既看得出又不刺眼。

**过渡/小结句的格式**:就用朴素段落——字号同正文、灰色、左对齐、无符号、不加粗、**不用斜体**(中文斜体是合成假斜,发丑)。别加竖线/彩色装饰,别缩小,否则和正文脱节、显得跳。

**标题的节号要和标题文字统一**:h2 的等宽节号别做成很淡的小灰字(会脱节),用**同标题的墨色 + 加粗 + 略大(`--fs-lead`)**,让「9」和「容器二·字典」读成一体。

## 七、页面骨架(每篇一致)

1. 顶部**进度条** + **常驻面包屑**(课程首页 — 01 — 02 — …,当前项蓝色)。
2. 两栏:左正文(读栏宽 ≤ 880px)+ 右**常驻目录**(按大模块 A/B/C/D 分组,滚动高亮当前节;窄屏自动隐藏)。
3. 章首:小标签 + h1 + 一段 recap(交代「从哪来、到哪去」)。
4. 正文按小节铺开。
5. 章尾:上一章 / 下一章两张导航卡。

全站**自足单文件**:CSS/JS 全内联,不挂任何 CDN,双击即看、离线可用、可拷 U 盘。

---

## 八、去下划线清单(治「下划线太多」)

- 链接:只留一条极淡下划线(`--acc` 30% 透明),hover 才变实。
- 强调:一律用**加粗**或**左竖线块**,不用下划线。
- 标题、术语、表头:**不准**下划线。

---

*抽取于 2026-08-13。2026-08-14 已封装成 skill `learning-notes-html`(在 `~/.claude/skills/`):设计系统 + 写作规矩-外链策展法 + Shiki/Mermaid 流水线 + template.html 成品骨架,范本 02-Python基础.html。以后写 03 起的章节直接用这个 skill。这份文档留作人读的底稿,skill 里是 consolidate 过的干净最终版。*

---

## 2026-08-19 增补(用户逐轮拍板 · 已全站铺开 · 详见 skill 的 design-system.md §8)

三条新约定,现在是基准线的一部分:

1. **背景 = 灰蓝卡纸底纹**(真实材质,不手搓):`body` 平铺 `assets/paper-linen-v3.jpg`(ambientCG 的 Fabric036 · CC0,提亮并调成灰蓝),`background-size:230px`,底色 `#e6eaf1`。**用真材质摄影、禁 feTurbulence 假纹;要别的纸材去那个材质库取**。深色模式试过后放弃。
2. **卡面微调**:纯白卡 `--surface` `#ffffff → #f6f8fc`(极淡冷白),和纸更融、不刺眼;course.css 旧页改 `--card`。
3. **外部资源升级成卡片**:section 标题「外部资源 · 骨架之上的血肉」,每条四行「是什么 / 为什么给你 / 精确看哪段 / 配我们哪节」+ 主线徽标 + 标签;组件类 `.res/.reslist`(CSS 见 skill)。**理念:本课程是骨架,外链是血肉——要写详细,让读者点进去就知道看什么**。
4. **第 1 章已从旧单栏重壳成两栏自足版式**(内容/交互/JS 不动);`.term` 的蓝色点状下划线已去掉(用户不喜欢)。

全站 30 个自足页 + course.css + index 已用一个幂等脚本铺完(每页留 `.prepaper.bak.html` 备份,现集中在 `_备份\`)。首页「本机环境档案」已改成"在任意机器上自检"的命令表 + `assets\检查环境.py` 脚本(换机不用再改首页)。
