# 维护约定

> 这份文件原本是 `README.md` 的内容(建库时写的目录说明 + 维护流程)。
> 2026-08-21 把 `README.md` 让给面向读者的项目首页之后,维护部分挪到这里,并按现状更新。
> 建库:2026-07-29 · 主题「冷靛蓝图」(冷灰蓝纸面 / 群青主色 / 深青辅色)

## 目录结构

```
人工智能-系统学习/
├── index.html                 # 唯一总入口:五个阶段、配套区、环境自检
├── README.md                  # 面向读者的项目首页
├── 维护约定.md                 # 本文件
├── 笔记基准线-设计系统.md        # 设计系统的正本(字号/颜色/组件/纸材)
├── assets/                    # 公共资源(所有页面共用)
│   ├── course.css             # 旧版统一样式(仍被几个入口页引用)
│   ├── course.js              # 公共交互:进度条 / 代码复制 / 清单记忆 / 回顶
│   ├── quiz-engine.js         # 题库引擎:quiz 页只需定义题目数组
│   ├── lab-runner.js          # 实验室的页面内 Python 运行器(Pyodide,按 import 自动装包)
│   ├── study-tracker.js       # 学习台账的采集端(记「这一章被打开过几次」)
│   ├── paper-linen-v3.jpg     # 全站纸纹背景(ambientCG Fabric036 · CC0)
│   └── 检查环境.py             # 换机后跑一次,核对工具是否齐全
├── 基础课程/                   # Stage 1:8 章
├── 深度课程/                   # Stage 2:D01–D17(Track A 经典 / B 深度 / C 大模型)
├── 应用工程/                   # Stage 3:A01–A07
├── 项目/                       # Stage 4:P1–P7
├── AI使用/                     # Stage 5:U01–U07(指挥 AI 干活这件事本身)
├── 实验室/
│   ├── index.html             # 实验室入口(按阶段分组)
│   ├── lab-01 … lab-28        # 28 个实验(lab-06 要 PyTorch,只能本机跑)
│   └── code/labNN/            # 每个实验的本机 .py(浏览器与本机同一份代码)
├── 题库/
│   ├── quiz.html              # 题库入口
│   └── quiz-01 … quiz-D17     # 39 份 · 347 题(基础 8 + 深度 17 + 应用 7 + AI 使用 7;项目不设)
├── 排障台/症状索引.html          # 按症状查的排障处方(17 条,不编号、随时增补)
├── 台账/学习台账.html            # 阅读 / 回查排行 / 题库错题 / 动手记录
├── 资料/                       # 资源库 · 学习路线 · 术语表 · 官方文档导航
├── skill/                     # 写这套笔记用的 skill(副本,正本在 ~/.claude/skills/)
└── _工具/                      # 八个自检脚本 + 同步 skill(不挂进索引,交付前跑)
```

## 使用方式(学习者)

1. 双击 `index.html`,一切从这里点进去。
2. 每章固定节奏:**读讲义 → 做实验 → 做题库 → 勾自查清单**,清单 ≥8 条才进下一章。
3. 实验有两种跑法:本机终端(推荐,真实工作流)或页面内运行器(Pyodide,浏览器里的真实 Python,首次需联网)。
4. 本机实验统一环境:`~\ai-lab` + `py -3.10 -m venv .venv`(建法见第 1 章第 8 节)。

## 约定(维护者 / 后续对话必读)

- **可移植性(2026-07-29 拍板)**:本库是跟人走的笔记,会被拷到 U 盘和其他电脑使用。
  库内所有链接一律相对路径;**正文命令与说明中禁止写死某台机器的绝对路径**
  (用 `~`、`%USERPROFILE%` 或「你的用户名」占位)。
  首页不再维护「某台机器的版本号」——换机跑 `assets/检查环境.py` 当场核对。
- **可视化优先(2026-07-29 反馈)**:图和可交互演示宁多勿少。每章至少 1 张概念关系图、
  1 张数据流/执行链图、1 个可动手的交互演示;交互必须标注「真实运行 / 机制模拟」。
- **版式参考基准**:排版有疑问时对照这三类范本,不自由发挥——
  交互式技术解说看 [Distill](https://distill.pub),章节结构与「文字-公式-代码」节奏看
  [《动手学深度学习》](https://zh.d2l.ai),参考资料页的信息层级看
  [MDN 中文](https://developer.mozilla.org/zh-CN/)。
- **教学标准**:零连续比喻;术语首现 = 标准名 + 一句白话;每个概念走 What/Why/Where +
  上游/数据/内部/输出/观察/易误解;数学逐符号讲并手算小例;代码必配预期输出与排错表;
  模拟标 `机制模拟`,真实运行标 `真实运行`,不得混淆。
- **题库标准**:九种题型混合;干扰项必须是真实常见误解,禁止一眼假;答案位置打散;解析要拆穿干扰项。
  **交付前必须跑 `_工具/题库自检.mjs`**——这条规矩 2026-07-22 就立过,只写在文档里不生效,
  2026-08 实测答案 86% 压在 B、93% 是最长选项。只有脚本挡着才算数。
- **口吻**:笔记是**第一人称独白**——「我发现」「我在 Claude Code 里见过」。
  不许对读者说「你」,也不许写成没有主语的说明书。**去掉「你」不等于改好了**:
  2026-08-21 就出过这个事故,620 个「你」被直接删掉、「我」一个没增、正文净减 812 字,
  而当时的脚本只查禁用词,判绿放行。现在脚本同时卡人称密度和字数基线。
- **不随意移动文件**;确需调整目录时全库同步改引用,并删除确认无引用的临时文件。

## 新增一章的完整清单

1. 新建讲义页(复用 `assets/` 与设计系统,见 `笔记基准线-设计系统.md`);
2. 需要动手的话,新建 `实验室/lab-NN-*.html` 与 `实验室/code/labNN/*.py`,
   并把每一步的「应该看到」**真跑一遍抓回来**,不要凭空写;
3. 新建 `题库/quiz-NN.html`(只写题目数组,引擎自动渲染);
4. 更新 `index.html`、`题库/quiz.html`、`实验室/index.html`、`资料/术语表.html`;
5. 更新本文件的目录树;
6. 质检——**七个脚本全绿才算完**:
   ```
   node _工具/全站体检.mjs      # 死链 0 / 锚点失效 0 / 缺底纹 0
   node _工具/口吻自检.mjs      # 0 处
   node _工具/题库自检.mjs      # 蒙题得分 = 随机基准,不合格文件 0
   node _工具/题目对正文.mjs    # 超范围的题标出来,逐条人工判断
   node _工具/覆盖体检.mjs      # 台账采集 46/46;题库/术语表/实验的缺口只报不拦(见「已知问题」)
   node _工具/实验自检.mjs      # 每段代码的「应该看到」必须等于真跑出来的输出
   node _工具/代码同步.mjs      # 页面里的代码块 和 code/labNN/*.py 必须是同一份
   ```
   第六个是 2026-09-11 加的:「应该看到不许凭空写」这条规矩写在文档里好几年了,
   但没跑过一次——一跑就查出 lab-02 有两段对不上、lab-01 有一段压根没写。
   它需要一台装了 numpy / pandas / scikit-learn 的 Python;缺包会标成「未验证」而不是假装通过。
   用法:`node _工具/实验自检.mjs [python路径]`,路径也能用环境变量 `LAB_PYTHON` 给。
   第五个是 2026-08-30 加的:前四个都只管「已写内容的质量」,没人管「该配套的有没有配」,
   结果题库 32/46、术语表 32/46、实验 13/46 三处洞全靠人眼扫才被发现。
   **注意:任何自检脚本都要先断言「样本数 > 0」**——题库自检就吃过这个亏(见下)。
   口吻自检现在多查两条(2026-08-21 事故之后加的):**讲义类每千字「我」≥ 1.2**,
   以及**正文字数不得低于 `_工具/正文字数基线.json`**——后者专门防「把词删掉当改好」。
   确属该删的内容,跑 `node _工具/口吻自检.mjs --rebase` 重记基线。
7. **改过 skill 的话**,跑 `node _工具/同步skill.mjs` 把正本同步进 `skill/`(单向,别改副本)。
   外加浏览器实渲染检查:对比度、溢出、窄窗口。

## 已知问题(欠账,登记在案)

- **46 章共用同一副章节骨架**,把不同性质的知识压平了(手感类、推导类、跑通类长得一样)。
  新的 AI 使用板块已经不套这副骨架,可以当重写时的参照。
- **D14 第 12 节**写「代码跑出来应该正好是 `[6.65, 3.45]`」,实测 `[6.6008, 3.3992]`。
- **第 1 章第 23 节**的资料表里 PyTorch 标「D03 章」、Hugging Face 标「阶段二 D04 起」,
  按现在的编号应为 D07 与 D12 起。

### 2026-08-30 全库体检新增欠账

- ~~**题库只覆盖 32 / 46 章**~~ —— **2026-09-10 已补齐**:新增 U01–U07 共 7 份 63 题,
  `题库/quiz.html` 也补上了 Stage 5 分区。现在 **39 份 / 347 题**(项目 P1–P7 仍按原设计不设)。
- ~~**术语表只覆盖 32 / 46 章**~~ —— **2026-09-10 已补齐**:新增「项目 · P1–P7」与「AI 使用 · U01–U07」两节共 23 条,
  现在 **46 / 46 章、166 条**。
- **实验室只覆盖 32 / 46 章**(2026-09-11 补了 lab-10~lab-28,配 D01、第 2/6/7/8 章、D03、D04、D08、D09、D13、D15、D11、D16、A01、A02、A03、A04、A06、A07)——
  但剩下的 14 章全部是**设计上不该有**的,别照着数字硬补:
  - **项目 P1–P7(7 章)**:项目本身就是动手交付物,再配一个实验是重复。
  - **AI 使用 U01–U07(7 章)**:这一整段明确不写代码(U01 原话「这一章不需要写代码」),
    它的「动手」是判断题与流程练习,不是可跑的脚本。
  - **欠账已清零(2026-09-14)**:讲师部分 32 章全部有配套实验(最后补上的是 D11 的 lab-28,
    用纯 numpy 手写冻结/微调,不需要 PyTorch)。
    其中 **只有 D11 需要 PyTorch**(得像 lab-06 一样标「只能本机跑」),
    **A01–A04、A06、A07 需要调 API**——真要做,得用桩模型代替真实调用(照 lab-09 的做法)。
  补的时候注意:`维护约定` 要求每一步的「应该看到」必须**真跑一遍抓回来**,不许凭空写——
  所以这不是批量生成的活,一次做一两个才稳。`实验室/index.html` 页尾已按这个口径重写。
- **残余字号未收敛**(都不在正文,不挡阅读,但严格说在 7 级之外):
  SVG 连线旁小字 `.el/.el-acc/.el-mono` 硬写 `12px`(62 页);Mermaid 预渲染 SVG 里的 `<text>` 也是 `12px`;
  `<details>` 的展开三角 `▸` 用 `9px`;窄屏 h1 覆盖用 `26px`。
  要彻底清零得重跑 Mermaid 渲染流水线,故暂留。

### 2026-09-12 新增第 7 个脚本:`代码同步.mjs`——页面代码与磁盘 .py 的漂移

- 实验页里写着「代码同时放在 `实验室\code\labNN\` 下」。这句一旦不成立,
  **照着「方式 B」走的人跑出来的输出就和页面上的「应该看到」对不上**——
  而`实验自检`只验页面里的代码,查不出这种漂移。
- 一跑就查出 **7 组不一致**:4 组是补实验时改了一边忘了另一边,3 组是 lab-01 历史遗留
  (页面重排过,磁盘还留着旧版:磁盘的 `step3_learn.py` 是完整解法,页面拆成了「最小版本 + 补全中」)。
- **以页面为准**:页面是学习者直接看到的,也已经过了口吻检查和输出比对。
  用 `node _工具/代码同步.mjs --fix` 一键同步(**会覆盖磁盘文件,先确认 git 干净**)。
- 两个判断边界:带「填入参考答案」按钮的是页面内练习题,不要求磁盘有副本;
  同名块(如 `step3_learn.py(最小版本)` / `(补全中)`)按「含『最小版本』的优先」挑,并打印出来让人确认。
- **这个脚本值得在动手改任何实验代码之后、提交之前跑一次。**

### 2026-09-11 又修掉一个假绿灯:`全站体检.mjs` 从来没有退出码

- 它检查死链和锚点失效,但**脚本结尾没有 `process.exit`——不论查出多少问题都 exit 0**。
  维护约定写着「四个脚本全绿才算完」,而这一道其实从来没拦过任何东西:
  期间积了 3 条死链(含 lab-16 指向还没建的 lab-17 的前向链接),一直是「报出来但不算数」。
  已补:①②⑤ 任意一项非空即退出码 1。**加上退出码的当场就报了 3 条死链。**
- 同类问题第三次出现(题库自检的 CRLF 假绿灯 → 实验自检的静默跳过 → 全站体检没有退出码)。
  **通用判据:一个自检脚本只有三条都满足才算数——① 有退出码 ② 样本为 0 时报警而不是判过
  ③ 解析失败时不静默跳过。** 加脚本时照着这三条核一遍。

### 2026-09-11 踩坑(两条都和「工具改了文件」有关)

- **预览工具往 HTML 里注入了 `data-page-node-id` 属性**:`实验室/lab-12-神经网络.html` 187 处、
  `资料/术语表.html` 932 处(术语表因此虚胖了 40KB)。注入本身不影响浏览器显示,但**会打断
  写死标签的解析**——`实验自检.mjs` 用的是 `/<div class="runner">/`,`lab-12` 的 div 变成
  `<div class="runner" data-page-node-id="…">` 之后**整份文件被静默跳过**,总数还看不出少了谁。
  已清掉注入、并把正则改成 `/<div class="runner"[^>]*>/`(容忍多余属性)。
  **教训:凡是「解析出 0 个」就跳过的地方,都要显式报错**——这和题库自检的假绿灯是同一类问题。
  **注意它是会复发的**:每在预览面板里打开一次页面,就可能被重新注入一轮
  (lab-15 被注入过两轮,第二轮 189 处)。所以这条检测已经加进 `全站体检.mjs` 的 ⑤,
  发现就报错。清的时候用 `/\s+data-page-node-id="[^"]*"/` **并回读验证**,别信自己的报告。
- **别用 `indexOf('文件名')` 定位代码块**:lab-01 的文件清单表格里也有同名文字,
  第一次就把 step4 的输出插到了 step1 后面。正确做法是让锚点带上后面的结构(文件名的下一段是 textarea),
  并且**改完先看一眼 diff**,别直接信脚本说「已改」。
- 改 HTML 里带换行的锚点时注意 CRLF:`\n` 匹配不上,用 `\s*` 或正则更稳。

### 2026-08-30 已修(登记以免复发)

- **`_工具/题库自检.mjs` 的假绿灯**:解析正则写的是 `;\n</script>`,只认 LF;
  而库里 98 个 HTML 是 CRLF,于是 **32 份题库全部「解析失败」、样本为 0,总闸 0/0 反而判过**。
  已改成 `;\s*<\/script>`(LF/CRLF 通吃),现在真跑出 162 道四选一 + 60 道判断、蒙题 70/222 对基准 70.5。
  **教训:任何「样本 0 → 判过」的闸门都是假闸门**,自检脚本要先断言样本数 > 0。
- **标题字号文档自相矛盾**:基准线文档与 skill 的 §1 表格都还写 18/22/30,
  而 skill §8 增补早已把标题提到 34/25/20。全站 63 页中 62 页已跟上,
  **只有样章 `02-Python基础.html` 停在旧值**(已补齐),三处文档也已统一为 34/25/20。
- **侧栏目录字号**:基准线写明「目录 = `--fs-micro` 11px」,实际硬写 `12px`,已改回变量(61 页)。
- **`资料/学习路线与计划.html` 停留在四阶段**:标题「四阶段总路线」、正文「39 章讲义」,
  与首页/README 的「五个阶段 46 章」矛盾,且路线图里没有 Stage 5。已补第五格与 Stage 5 清单。
- **首页实验室卡片**:原来 10 条竖排(入口 + 9 个实验),比隔壁题库/资料卡(各 4 条)高出一大截。
  已收敛成入口 + 3 条精选,与两张兄弟卡对齐;9 个实验仍在 `实验室/index.html` 里按阶段分组。
